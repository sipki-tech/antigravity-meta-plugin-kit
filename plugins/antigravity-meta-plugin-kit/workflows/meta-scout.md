---
description: Check the installed Antigravity for drift against the documented plugin contracts (after CLI/IDE updates or unexplained plugin failures)
---

1. Spawn the `meta-trap-scout` subagent; pass the user's concern (or "full sweep after an update") verbatim.
2. Relay its DRIFT / CONFIRMED / UNKNOWN report with the evidence quotes intact.
3. If DRIFT items exist, offer to update `docs/internals.md` of the meta-plugin-kit checkout with new observation dates.
